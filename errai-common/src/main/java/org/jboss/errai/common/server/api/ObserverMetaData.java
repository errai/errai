package org.jboss.errai.common.server.api;

/**
 * This interface is a placeholder type for encoding observer metadata from client code into the class model that
 * runs on the server, so all known observer endpoints can be known at runtime.
 *
 * @author Mike Brock
 */
public interface ObserverMetaData<T> {
}
