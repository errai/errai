TODO

Usage:
```xml
<plugin>
  <groupId>org.jboss.errai</groupId>
  <artifactId>errai-maven-plugin</artifactId>
  <version>${errai.version}</version>
  <executions>
    <execution>
      <phase>process-resources</phase>
      <goals>
        <goal>errai</goal>
      </goals>
    </execution>
  </executions>
</plugin>
```
